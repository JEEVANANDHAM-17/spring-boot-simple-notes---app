# Copy this file to .env and enter your local MySQL credentials.
# The .env file is ignored by Git and must never be committed.
DB_USERNAME=root
DB_PASSWORD=replace-with-your-local-password

# Optional: override the default connection URL.
# DB_URL=jdbc:mysql://localhost:3306/easy_notes?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
